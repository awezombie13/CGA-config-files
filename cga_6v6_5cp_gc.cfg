// 5cp GOLDEN CAP config for CGA 6v6
// Created by cre-8
// Config originally created May 29, 2020
// EXEC THIS MANUALLY, DO NOT EXEC THE BASE.CFG

sm plugins unload disabled/tf2Halftime // unloads tf2Halftime plugin as the config manages things just fine

// exec "cga_6v6_base"                  // don't need to exec base cfg again, teams don't typically changelevel after 1st/2nd half anyway

servercfgfile 	"cga_6v6_5cp_gc"        // sets server cfg to this config so that it gets autoexeced on level change (if that happens)

mp_timelimit "0"                       // unsets server timelimit
mp_winlimit "1"                        // sets # of wins to win the match to >1<
mp_windifference "0"                   // unsets windifference
mp_maxrounds "0"                       // unsets maxrounds

mp_tournament_restart                  // restarts the tournament to apply above settings

say "CGA 6v6 GOLDEN CAP 5cp Config Executed."
say "First team to cap the other's last point wins the match. There is no time limit."
say "restart your server 90 seconds after you have fully finished playing."